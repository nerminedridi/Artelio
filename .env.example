PORT=3000
NODE_ENV=development

DATABASE_URL=postgres://user:password@localhost:5432/artelio

SESSION_SECRET=replace-with-a-long-random-string

CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Optional — without this, emails (welcome, password reset, newsletter) are
# logged to the console instead of actually sent.
RESEND_API_KEY=
EMAIL_FROM=ARTélio <onboarding@resend.dev>

# Dedicated database for npm test - wiped and reseeded every run.
TEST_DATABASE_URL=postgres://user:password@localhost:5432/artelio_test
